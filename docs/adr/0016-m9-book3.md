# ADR 0016 — M9 Book 3 (Flowers of Esthelm)

Date: 2026-09-26 · Status: plan accepted (user, 2026-09-26); M9.1 and M9.2 merged (tags `m9.1-done`, `m9.2-done`); M9.3 in progress.

## User choices (plan)
- M9 = Book 3 canon. Book 3 in four canon batches, one branch + PR each: M9.1 3.00 E – 3.05 L + 1.00 D / 1.01 D · M9.2 3.06 L – 3.14 · M9.3 3.15 – 3.20 T · M9.4 3.21 L – 3.25.
- Laken (Riverfarm) and Geneva (Baleros) are events only, like Tom on Rhir (M8.3): news and rumors, no map.
- Esthelm becomes a playable map (`esthelm_ruins`, from the road camp) with the siege as a wave stage (M9.3).
- Lyonette and Mrsha get `npc_behaviour` entries in M9.1; Zel later (M9.4). Data only, no schema change.
- Stage and hook choices are asked at the start of each batch. Any schema or engine change is asked first.

## Book 3 facts that shape the data
- Book 3 of the ebook: 28 chapters, 305,675 words, extracted to `canon/raw/book3` (gitignored). 1.00 D / 1.01 D sit between 3.05 and 3.06 in the spine.
- "E" in 3.00, 3.01, 3.11 and 3.12 is Laken Godart ([Emperor]), not Erin. His "Day N" counts from his own arrival, and the text gives no safe link to the game calendar (his snow and his avalanche point to different offsets). So Laken events use placeholder days from day 71 on, in order, like the Rhir thread (1.00C); each is `"confidence": "guess"` and its note gives his diary day. This also keeps Book 3 events out of the Book 1 and Book 2 test runs.
- 1.00 D / 1.01 D follow Geneva Scala ([Doctor]) in Baleros. Placeholder days (72–76), like 1.00C.
- Name clashes, kept apart: the maid Teresa (3.13) is not `teresa`; the dead Horn Marian (3.08) is not `marian`; Laken's Earth friend Teresa (3.00) is not an NPC. The Hob in Rags' tribe in 3.10 is unnamed and is not linked to `garen`.
- Erin stays in Celum for all of Book 3. In 3.25 she leaves on the wagon south with the Horns; she is home only in Book 4. The Albez door as a portal is Book 4 work.
- Ryoka never gains a level. The Florist's Horror ranks and level loss are `other` system records, not a new mechanic.
- Book 3 ends near day 88–90 (a guess). Winter lasts the whole book.

## M9.1 Canon 3.00 E – 3.05 L + 1.00 D / 1.01 D
User choices (2026-09-26): two stages — Ryoka against Persua and the Runners in the Celum Runners' Guild (combat, the player can join), and Lyonette reopening the inn (scene, with a hook for a player who eats or works there). Ryoka gets an NPC schedule in Celum.

**World data (by hand).** Enemies `persua_courier` (escapes below 40% HP; she never dies on the map) and `celum_runner`. `npc_behaviour` entries: `lyonette` (inn kitchen and common room, from `lyonette.works_at_inn`), `mrsha` (at the inn from `mrsha.at_the_wandering_inn`, off the map while `mrsha.in_selys_care`), `ryoka_griffin` (Celum from `ryoka.in_celum`: the Runners' Guild in the morning, deliveries off the map, the Frenzied Hare in the evening; leaves on `ryoka.gone_to_magnolia`, set in M9.2). Lyonette and Ryoka get combat blocks (design numbers; Ryoka still never levels). A counter at the Liscor inn with shop `wandering_inn` (hot meal, bread); it is always open, which the text does not say.

**Canon data** (drafted by a delegated agent that read the chapters outside the main session, then re-verified in the main session). 39 events in 8 chapter files: 3.00E 4, 3.01E 4, 3.02H 4, 3.03 6, 3.04 6, 3.05L 7, 1.00D 3, 1.01D 5. New NPCs: `laken_godart`, `durene`, `prost`, `yesel`, `ivolethe`, `geneva_scala`, `okasha`, `thriss`, `belgrade`, `anand`, `garry`. New locations: `riverfarm`, `celum_adventurers_guild`; Albez is the book1 `ruins_of_albez`. Persua's book1 record gains the 3.04 note ([Double Step], her oath against Ryoka).

**Timeline (guesses).** Laken (placeholder) 71–74; the Horns in Albez 72; the Goblin army passes Liscor the night of 72; Celum 74 (three days after Erin arrived) and 75 (the guild fight, 10–14); Lyonette reopens on 76 (scene, 18–22); Geneva (placeholder) 72–76. No Laken or Geneva event depends on a Liscor or Celum event.

**Stages and hooks.** `b3.ryoka_beats_persua_in_the_runners_guild` (combat; Persua and three Runners; Ryoka in wave 0; hook `player_stood_with_ryoka`: Ryoka +3, Persua −2, a news line). `b3.lyonette_reopens_the_inn` (scene; Lyonette and Pawn; hook `player_ate_at_lyonettes_inn`: buying or working at the inn on days 72–76, Lyonette +3, a news line).

**Flags for later batches.** `horns_of_hammerad.gone_to_albez` (Pisces' Floodplains goal now stops on it; clear it in 3.24), `ryoka.in_celum` stays (M9.2 sets `ryoka.gone_to_magnolia`), `persua.swore_to_kill_ryoka`, `wandering_inn.reopened_by_lyonette`, `lyonette.runs_the_inn`. `pawn.back_in_hive` is not cleared: 3.05 shows one visit only.

**Flagged, not linked.** Laken's snow day and his later chapters point to different offsets (hence placeholders). Olesm says the Goblin army sacked Esthelm; Book 2 gives the burning to Rags' tribe; the army's leader is not linked to the Goblin Lord. Geneva's Level 17 is worked out (11 + 6), marked `likely`. The Watch asks Ryoka to leave Celum, but she stays until 3.09. The old Runners' Guildmaster is not linked to `eterell`.

**Tests.** New `sim_canon_book3` (4 tests: every `b3.` event to day 76 with drift 0, news, end state, the far threads stand alone) and `sim_book3_stages` (7 tests: both stages start on the real data, Persua escapes and never dies, the scene places Lyonette and Pawn, both hooks change their events). `unit_combat_db`: 25 enemies. `sim_player_hooks`: 14 hooks. GUT 602/602 (65 scripts); validator 0 errors (`--all`); Python 51/51. Checked on screen (the guild fight on day 75, 10:00).

## M9.2 Canon 3.06 L – 3.14
User choices (2026-09-26): a scene stage — Ryoka and Fals at the Frenzied Hare on the morning of day 76 (3.09), with a hook for a player who serves or sits with them; and Corusdeer soup as a real food item that keeps the cold off (an engine change, accepted).

**Corusdeer soup (engine, save v12).** `economy.json` goods may carry two new optional fields: `warm_minutes` (only on a `food` or `eat_now` good: eating it keeps the cold off for that long, `Winter.warm_for`) and `from_flag` (shops list and sell the good only once that flag is set, `EconomyDb.on_sale`). `WinterState.warm_until` (world second, -1 = none) is the new save field; `Winter.is_warm` is true until then, and the cold after a meal counts only the time past `warm_until`. `SAVE_VERSION` 12, migration 11→12 sets `warm_until` to -1. The good `corusdeer_soup` (8c, food, 240 minutes, `from_flag: erin.makes_corusdeer_soup`) is sold at Stitchworks. Shop, price and hours are guesses; the canon (3.09) is only that Erin and Octavia make it in Octavia's shop and it keeps Ryoka warm.

**World data (by hand).** `npc_behaviour` entry `fals` (the Celum Runners' Guild 6–9, then runs off the map; hours are guesses) so the scene can place him. At 7–10 he stood in the way of the day-75 guild fight, so his hours end before it.

**Canon data** (drafted by a delegated agent that read the chapters outside the main session; class and Skill names re-checked in the main session). 41 events in 9 chapter files: 3.06L 7, 3.07H 4, 3.08H 4, 3.09 6, 3.10 4, 3.11E 4, 3.12E 2, 3.13 5, 3.14 5. New NPCs: `nemor` (killed by Magnolia in 3.14), `frostwing`, `gamel`. New locations: `ocre`, `first_landing`, `road_to_invrisil` (guess, for the Goblin tribe on the road).

**Timeline (guesses).** Horns: the dungeon 73–74, out of Albez and into Ocre 74 (`horns_of_hammerad.trapped_in_albez` cleared), the parade 75. Celum, Ocre, the coach and Magnolia's house: all day 76; Magnolia leaves for First Landing on 77. Pawn: 76–78, the Soldiers die for Heaven on 79. Laken placeholders 75–80 (his Day 31 → 75 … Day 45/46 → 80); the avalanche (78) has no `depends_on`.

**Stage and hook.** `b3.ryoka_and_fals_talk_at_the_frenzied_hare` (scene, day 76, 9–12; Ryoka and Fals; Garia is not there, Agnes has no schedule). Hook `player_sat_with_ryoka_and_fals`: `serve_guests`, `talk_with_guest` or `buy_supplies` at the Frenzied Hare on day 76 → Ryoka +2, Fals +2, a news line.

**Flags for later batches.** `ryoka.gone_to_magnolia` (3.10, ends her Celum schedule), `ryoka.banned_from_runners_guild`, `erin.makes_corusdeer_soup`, `horns_of_hammerad.in_ocre`, `horns_of_hammerad.has_albez_treasure`, `horns_of_hammerad.hold_forty_gold_for_erin`, `yvlon.armor_fused_to_arms`, `magnolia.allied_with_ryoka`, `magnolia.gone_to_first_landing`, `pawn.cares_for_the_soldiers`, `pawn.allowed_to_pray`, `laken.rules_riverfarm`, `laken.left_for_invrisil`, `rags.tribe_flees_north`. `horns_of_hammerad.gone_to_albez` is still not cleared (3.24).

**Flagged, not linked.** The Horns' timeline does not fit (Fals hears of the parade the same morning Ceria says not a day has passed). Riverfarm has about sixty people in 3.00E but over a hundred in 3.12E. The "Ylawes Byres" persona under the Albez spell, the dead Horn Marian and the maid Teresa are not linked to any NPC. The Hob with the crimson sword is unnamed (not `garen`). Laken's level line says [Beast Tamer] where he calls it [Beastmaster]; the data uses [Beast Tamer]. Pawn gains no level on screen; the number of dead Soldiers is not given. Nemor's race is `likely` Human.

**Tests.** `unit_economy` +2 (the soup goes on sale by flag; it feeds and warms, then wears off; data errors), `unit_winter` +2 (a warm meal; a v11 save loads), `sim_book3_stages` +3 (the Hare scene; the hook; soup on sale and Ryoka gone), `sim_canon_book3` to day 80, `sim_player_hooks` 15 hooks.

## M9.3 Canon 3.15 – 3.20 T
User choices (2026-09-26): the Esthelm siege as a wave stage where the player holds Ylawes' barricade and the Redfang band comes in a late wave on the player's side; and Erin's play at the Frenzied Hare (3.16) as a scene stage with a hook.

**World data (by hand).** New map `esthelm_ruins` (location `esthelm`; torn-down walls, a forced east gate, a rubble maze, and Ylawes' barricaded north-west quarter with one palisade gate). A side road from the road camp's east edge (180 minutes; a guess). Knock-out wake point in the ruins. `npc_behaviour`: off-map place `esthelm` and `ylawes_byres` (guard post at the palisade gate from `ylawes.at_esthelm`; combat block is a design number for a Gold-rank [Knight]). Nine stage-only enemies: `goblin_lord_soldier`, `goblin_lord_archer`, `goblin_lord_shaman`, `goblin_lord_hob`, `goblin_lord_commander` (rider and Shield Spider are one foe), and the helpers `esthelm_defender`, `redfang_warrior`, `redfang_hob`, `silver_rank_adventurer`. No new tile, no schema or engine change.

**Canon data** (drafted by a delegated agent that read the chapters outside the main session; class and Skill names re-checked in the main session). 37 events in 6 chapter files: 3.15 5, 3.16 6, 3.17T 7, 3.18T 6, 3.19T 7, 3.20T 6. New NPCs: `jasi`, `hess`, `ylawes_byres`, `esthelm_florist`, `goblin_vanguard_commander`, `goblin_vanguard_head_shaman`, and the Redfang band (`grunter`, `badarrow`, `headscratcher`, `bugear`, `rocksoup`, `numbtongue`, `rabbiteater`, `shorthilt`, `leftstep`, `bitefly`, `orangepoo`, `patchhelm`, `justrust`). New locations: `celum_watch_house`, `celum_poor_quarter` (guess), `verdun`, `esthelm_sewers`, `esthelm_barricade_quarter`.

**Timeline (guesses).** Celum: 3.15 day 77 (Erin nearly a week in Celum, Ryoka gone about a day), 3.16 day 78 (the play that night). Esthelm: Toren and the Redfang band on the road 77–79, Toren enters at dusk on 79, the dead rise that night, the dawn kill teams and the last battle on day 80. The battle waits for the dawn events of the same day (`depends_on` the Florist's last event) so Grunter is still alive for them.

**Stages and hooks.** `b3.erin_stages_romeo_and_juliet` (scene, day 78, 19–23; Erin and Wesle; the rest of the cast has no schedule). Hook `player_watched_erins_play`: `serve_guests`, `talk_with_guest` or `buy_supplies` at the Hare on day 78 → Erin +2, Wesle +1, a news line. `b3.the_last_battle_of_esthelm` (fight, day 80, 8–18, needs `ylawes.at_esthelm`): a kill team of five at the east gate; wave 0 Ylawes and four defenders; then six dead from the south, the main assault (eight), the Redfang band (a Hob and five, helpers), the commander with two soldiers, and last four Silver-rank adventurers as the last foes fall. 22 foes; the records name the commander. Hook `player_held_the_esthelm_barricade` → Ylawes +3, Headscratcher +1, a news line. Toren and his running zombies fight everyone, so they are not placed; neither is the Horror.

**Flags for later batches.** `toren.heading_to_liscor`, `redfang_band.left_esthelm`, `silver_swords.at_esthelm`, `esthelm.saved`, `goblin_lord.vanguard_routed`, `jasi.works_at_frenzied_hare`, `grev.lives_at_frenzied_hare`, `erin.makes_thickskin_soup`, `erin.staged_a_play`, `agnes.wants_jasi_gone`. Deaths: Grunter, Rocksoup, Leftstep, Orangepoo, Bitefly, Patchhelm, Justrust, the commander (battle), the Florist (Toren), the head Shaman (Toren).

**Flagged, not linked.** Ylawes is not linked to Yvlon: these chapters never call them family (Yvlon's book1 record says her brother is a Gold-rank [Knight]; noted). The Silver Swords' half-Elf and Dwarf are unnamed here; no wiki names. The Florist's own day count (ninth day after the fire) would be day 80, but she is already changed on day 79; her first feeding is put on day 78. The army is "hundreds" in most lines, "thousands" once. Toren's [Barmaid] class is in the text (3.18T).

**Tests.** New `sim_esthelm_siege` (6 tests: the map and stages load; the waves come in order with Ylawes, the defenders and the Redfang band on the player's side; holding the barricade changes the battle; no siege on day 79; the play scene places Erin and Wesle; watching the play changes it). `sim_canon_book3` end state for M9.3; `unit_combat_db` 34 enemies; `sim_player_hooks` 17 hooks. GUT 615/615 (66 scripts); validator 0 errors (`--all`); Python 51/51. Not checked on screen.
