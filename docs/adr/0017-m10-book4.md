# ADR 0017 — M10 Book 4 (Winter Solstice) and the Albez door

Date: 2026-09-26 · Status: plan accepted (user, 2026-09-26); M10.0 merged (tag `m10.0-done`); M10.1 built.

## User choices (plan)
- M10 = Book 4. The Albez door comes first (M10.0), then five canon batches, one branch + PR each:
  - M10.1: 3.26 G – 3.29 G
  - M10.2: 3.30 – 3.31 G + Wistram Days 1–7
  - M10.3: 3.32 – 3.35
  - M10.4: 3.36 – 3.39
  - M10.5: 3.40 – 3.42 + Interlude – Winter Solstice
- The seven "Wistram Days" interludes show Ceria and Pisces at Wistram in the past. They are history and NPC notes only. They put no events on the calendar, like the Antinium Wars interludes (M8.7).
- The Albez door is a real door the player can use. The engine and save change was accepted.
- The Celum end of the door is a new Stitchworks map (Octavia's shop), not the Frenzied Hare.
- The player gets 0 trips before Erin's [Magical Grounds] (Level 30, 3.33), then 4 trips a day.
- We ask for stage and hook choices at the start of each batch. We ask before any other schema or engine change.

## Book 4 facts that shape the data
- Book 4 of the ebook has 25 chapters and 282,404 words. It is extracted to `canon/raw/book4` (gitignored).
- Door facts (read by a delegated agent, 3.30–3.34):
  - 3.30: Erin moves the Celum anchor from the Frenzied Hare to Octavia's shop, because she does not trust Agnes with it. The artifact door rides on the wagon. The door drinks mana. Before Erin levels, mages with potions can power only one one-way trip for four people.
  - 3.32–3.33: the wagon reaches Liscor at sunset, after a night at the Hare and a night at Esthelm. The door stands in the inn's common room.
  - 3.33: the door runs dry and shuts while in use, and five mages reopen it. Erin reaches Level 30. Her [Magical Grounds] Skill fills the inn with mana. Pisces guesses the door can then carry 16–30 people a day.
  - 3.34 onward: a second door is set in Octavia's shop. The only link in Book 4 is Liscor–Celum.
  - Anyone may use the door. Erin charges no fee.
- 3.25 set `albez_door.linked_to_frenzied_hare`. This was true only until 3.30, which moves the anchor to Octavia's shop. M10.2 clears the flag.
- Toren in Book 4:
  - His death is never shown.
  - 3.27M has a weak skeleton near Mrsha. The text does not name it.
  - In 3.32, someone says Goblins killed a purple-eyed skeleton at Esthelm. The text does not confirm it is Toren.
  - Keep him alive unless the text says he died.

## M10.0 The Albez door (engine, save v13)
**Map objects.** There are two new optional fields:
- `when_flags` / `unless_flags`: the object is on the map only while the flags hold.
  - `MapDb.sync_flags` keeps the set of hidden objects. The set is part of `overlay_key`, with the overlays.
  - `objects_near` and the new `objects_on` skip hidden objects.
  - The map view does not draw hidden objects.
  - An object with flags must not be solid.
- `portal`: `{"to", "pos", "power_flags"}` makes the object a magic door. The map validator checks:
  - the target map;
  - that the arrival tile is walkable;
  - the flag list;
  - that `rules.portal` and the action `travel` exist.

**Rules.** There is a new optional section, `rules.portal`, with these fields: `trips_per_day` (4), `minutes` (1), `line`, `dry_line` and `spent_line`. In the text a trip is instant. Here it takes one minute, so that it is a travel action.

**Portal.** `core/portal.gd` holds `Portal.use`. You call it with `Commands.portal`, the use menu or the console command `portal <object>`.
- It refuses when a power flag of the door does not hold (`dry_line`).
- It refuses when the player has no trips left today (`spent_line`).
- Otherwise it logs a travel action with `context.portal`, counts the trip and moves the player. The player feels no cold on the way, as on a covered ride.

**Save v13.** `PlayerState` has two new fields, `portal_day` (-1 = none) and `portal_trips`. Migration 12→13 adds them.

**World data.**
- A new indoor map, `celum_stitchworks` (location `stitchworks`). It holds Octavia's counter (the Stitchworks shop) and the door.
- A door tile and an exit from Celum square at (1, 13).
- Two door objects, both powered by `erin.magical_grounds`:
  - `albez_door` in the inn, shown by `albez_door.at_wandering_inn`;
  - `albez_door_celum` in Stitchworks, shown by `albez_door.anchor_at_stitchworks`.
- The canon batches set these flags:
  - M10.2 (3.30) sets the anchor flag.
  - M10.3 (3.32–3.33) sets the inn flag and `erin.magical_grounds`.
- The map size, the layout and the door places are guesses.

**Tests.** `unit_portal` is new. Its 11 tests check that:
- the door data loads;
- there is no door before its flags are set;
- a dry door stays shut;
- the player can step through and back;
- the player gets four trips, and more on the next day;
- the Celum end needs its own flag;
- the shop door leads in, and the counter sells;
- the trip count is saved;
- a v12 save loads;
- bad portal data and bad portal rules are reported.

`unit_interact` expects the new `portal` option field.

**Results.**
- GUT: 640/640 (68 scripts).
- On screen: the door shows in the inn, and the use menu offers the trip.

## M10.1 Canon 3.26 G – 3.29 G
User choices (2026-09-26):
- Two scene stages with hooks, both on day 85 (3.27M):
  - Lyonette searches the snow for Mrsha on `floodplains_south` (11:00-16:00). Talking to her or comforting her there changes the event.
  - The rescuers gather at the new `dungeon_rift` map (14:00-20:00). Keeping watch at the rope anchor, or talking there, changes the rescue.
- The captives in Tremborag's mountain get one neutral line only (in the breakout summary).
- Our spellings win: `griffon_hunt` (the text says Griffin Hunt) and Jawbreaker / Rockgaw (the text says Rockjaw). The book spelling is in `canon_ref.note`.
- Rags' days chain from a guessed day 85: arrival 85, Ulvama 86, the raid 87, Noears 88, Pyrite and the breakout 89, the captives and pike squares 90.

**Data.**
- New folder `game/data/canon/book4/`: 17 events in 3.26G, 3.27M, 3.28G and 3.29G.
- 6 new NPCs: `tremborag`, `ulvama`, `noears`, `pyrite`, `redscar`, `greybeard`.
- 3 new locations: `north_izril` (region), `tremborags_mountain`, `liscor_dungeon_rift`.
- 3.27M is day 85: Zel came "last night" (3.23L, day 84). So it lands before 3.24 / 3.25. That is fine for the calendar.
- The Rags chapters are off the map. Unnamed Human towns and camps use `north_izril`.
- Toren lives. In 3.27M he is weak, has a sword again, and watches Mrsha fall (`toren.spared_mrsha`, `toren.in_liscor_dungeon`). `toren.heading_to_liscor` stays set.
- The rescue depends on the fall, not on Lyonette's search. If Lyonette is dead, Mrsha is still found.
- Toren's event does not need `mrsha.fell_into_the_dungeon`: the rescue runs first in the run order and clears it.
- The unnamed Lord of the Wall in 3.27M is not linked to Ilvriss. Yaltes Byres is history only, not linked to Yvlon or Ylawes.

**World data.**
- New map `dungeon_rift` ("Edge of the rift"), south of `floodplains_south` (60 minutes). The place, size and shape are guesses.
- New tile `chasm` (not walkable). A knock-out on the rift wakes the player at the inn.
- The rope anchor object offers `keep_watch`.
- Fix: Rags no longer forages on the floodplains after `rags.tribe_turns_north` (day 71).

**Tests.**
- `sim_canon_book4` (new): Book 4 runs from day 85 to day 90 with drift 0. It also checks that Rags stays away, that Mrsha is found without Lyonette, and that without Tremborag the north is quiet.
- `sim_book4_mrsha` (new): both scenes, both hooks, and no scene on another day.
- `sim_player_hooks`: 24 hooks.

**Results.**
- GUT: 651/651 (70 scripts). Python tool tests: 51 pass. Validator: 0 errors.
- On screen: the rift scene draws Zel, Klbkch, Krshia and Lyonette at the edge.
